# Production requirements for What DAWG!?
# Optimized for deployment (CPU-only PyTorch)

# PyTorch CPU-only (smaller size for deployment)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# Web Framework
flask>=3.0.0
werkzeug>=3.0.0
gunicorn>=21.0.0

# Image Processing
Pillow>=9.0.0

# Utilities
numpy>=1.21.0
